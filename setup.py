from setuptools import setup, find_packages

setup(
    name="unsupervised-dl",
    version="0.1.0",
    license="unlicense",
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)
